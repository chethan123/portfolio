# The gate's allowlist, and the only authorization this instance has: one Google
# address per line, blank lines and `#` ignored. Copy to `allowed-emails.txt`.
# Removing a line signs that person out on their next request. Restart `gate`
# after editing — a single-file bind mount stops following a file replaced by rename.

alice@gmail.com
bob@gmail.com
