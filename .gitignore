# --- Portfolio Tracker --------------------------------------------------------

# React Router build output (client and server bundles) — rebuilt by `npm run
# build` and by the Dockerfile's build stage.
build/

# Generated route types (`react-router typegen`). Regenerated by `npm run
# typecheck`, so there is nothing to gain from committing them.
.react-router/

# Real configuration. `.env.example` is the documented surface and is committed;
# a `.env` holds this instance's secrets and is not.
.env
.env.local

logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

pids
*.pid
*.seed
*.pid.lock

lib-cov

coverage
*.lcov

.nyc_output

.grunt

bower_components

.lock-wscript

build/Release

node_modules/
jspm_packages/

web_modules/

*.tsbuildinfo

.npm

.eslintcache

.stylelintcache

.node_repl_history

*.tgz

.yarn-integrity

.env
.env.*
!.env.example

.cache
.parcel-cache

.next
out

.nuxt
dist
.output

.cache/

.vuepress/dist

.temp

.svelte-kit/

**/.vitepress/dist

**/.vitepress/cache

.docusaurus

.serverless/

.fusebox/

.dynamodb/

.firebase/

.tern-port

.vscode-test

.pnpm-store

.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.vite/

# Ephemeral git worktrees an agent checks the repo out into. `.claude/skills/`
# is tracked; these are not — they are copies of this repository, created per
# task and removed after it.
.claude/worktrees/
