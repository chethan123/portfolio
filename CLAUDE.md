Claude Code doesn't read `AGENTS.md` natively — this file exists only to import it, so the
guidance below actually reaches a Claude Code session. Edit [`AGENTS.md`](AGENTS.md), not this
file.

@AGENTS.md
